export function playDice(p:{betAmount:number; payload?:{mode?:'over'|'under'; target?:number}}){ const mode=p.payload?.mode||'over'; const target=p.payload?.target||50; const roll=Math.floor(Math.random()*100)+1; const win=mode==='over'?roll>target:roll<target; const chance=mode==='over'?100-target:target; const multiplier=chance>0?Math.max(1.01,98/chance):0; const winAmount=win?Math.floor(p.betAmount*multiplier):0; return {game:'dice',roll,mode,target,win,multiplier,winAmount} }
