import { FastifyInstance } from 'fastify'
import { z } from 'zod'
import { prisma } from '../db.js'
import { parseTelegramUser, verifyTelegramInitData } from '../auth/telegram.js'
export async function authRoutes(app:FastifyInstance){
 app.post('/telegram', async(req,rep)=>{ const {initData}=z.object({initData:z.string().min(1)}).parse(req.body); if(!verifyTelegramInitData(initData,process.env.TELEGRAM_BOT_TOKEN!)) return rep.code(401).send({error:'Invalid Telegram initData'}); const tg=parseTelegramUser(initData); if(!tg)return rep.code(400).send({error:'Telegram user missing'}); const user=await prisma.user.upsert({where:{telegramId:BigInt(tg.id)},update:{username:tg.username,firstName:tg.first_name,lastName:tg.last_name,photoUrl:tg.photo_url},create:{telegramId:BigInt(tg.id),username:tg.username,firstName:tg.first_name,lastName:tg.last_name,photoUrl:tg.photo_url,balance:1000000n}}); const token=app.jwt.sign({userId:user.id,telegramId:user.telegramId.toString()}); return {token,user:{id:user.id,telegramId:user.telegramId.toString(),username:user.username,firstName:user.firstName,lastName:user.lastName,photoUrl:user.photoUrl,balance:Number(user.balance)}} })
 app.post('/dev', async(req,rep)=>{ if(process.env.NODE_ENV!=='development') return rep.code(404).send(); const {telegramId}=z.object({telegramId:z.number().int()}).parse(req.body); const user=await prisma.user.upsert({where:{telegramId:BigInt(telegramId)},update:{},create:{telegramId:BigInt(telegramId),username:'dev_user',firstName:'Dev',balance:1000000n}}); const token=app.jwt.sign({userId:user.id,telegramId:user.telegramId.toString()}); return {token,user:{id:user.id,telegramId:user.telegramId.toString(),username:user.username,firstName:user.firstName,lastName:user.lastName,photoUrl:user.photoUrl,balance:Number(user.balance)}} })
}
